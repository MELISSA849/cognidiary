# SmartDiary - Standalone Intelligent Diary Application
# Minimal dependencies for maximum compatibility and efficiency
# No external heavy dependencies like TensorFlow or complex ML frameworks

# Core Web Framework (lightweight, stable versions)
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1

# Basic Image Processing (lightweight, no heavy ML)
Pillow==10.1.0
opencv-python-headless==4.8.1.78
numpy==1.26.2

# Speech Recognition (primarily uses browser APIs, minimal backend)
SpeechRecognition==3.10.0

# All other functionality uses built-in Python modules:
# - datetime, json, os, logging, tempfile, io, glob, re, base64
# - Simple emotion detection using OpenCV Haar Cascades (built-in)

# Development Tools (optional, commented out)
# pytest==7.4.3
# black==23.11.0

# Installation Notes:
# ----------------
# This version is standalone and doesn't require:
# - TensorFlow, PyTorch, or other heavy ML frameworks
# - External audio codecs (ffmpeg, pyaudio)
# - System-level dependencies
# - Compilation tools
#
# All features work using:
# - Browser native speech recognition API
# - OpenCV built-in Haar Cascades for face detection
# - Simple rule-based emotion analysis
# - Pure Python implementations where possible
#
# Install with: pip install -r requirements.txt
